
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Success - There's Something About AI</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700;900&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/main.css">
    <link rel="stylesheet" href="css/auth.css">
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="container">
            <div class="header-content">
                <a href="index.html" class="logo">
                    <div class="logo-icon">AI</div>
                    <h1 class="logo-text">There's Something About AI</h1>
                </a>
                <nav class="nav">
                    <a href="index.html" class="nav-link">Home</a>
                    <a href="instructions.html" class="nav-link">Instructions</a>
                    <a href="about.html" class="nav-link">About</a>
                    <a href="roadmap.html" class="nav-link">Roadmap</a>
                    <a href="profile.html" class="nav-link profile-link">Profile</a>
                </nav>
                <button class="mobile-menu-btn" id="mobileMenuBtn">☰</button>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="main auth-main">
        <div class="container">
            <div class="auth-container">
                <div class="auth-card">
                    <div class="success-content">
                        <div class="success-icon">✅</div>
                        <h1 id="successTitle">Success!</h1>
                        <p id="successMessage">Your operation completed successfully.</p>
                        
                        <div class="success-actions">
                            <a href="profile.html" class="auth-submit-btn">Go to Profile</a>
                            <a href="index.html" class="cancel-btn">Back to Home</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <h3>There's Something About AI</h3>
                <p>Compare and analyze GenAI responses with sophisticated semantic analysis</p>
                <div class="footer-bottom">
                    <p>© 2024 There's Something About AI. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>

    <script src="js/main.js"></script>
    <script>
        // Handle different success scenarios
        document.addEventListener('DOMContentLoaded', function() {
            const urlParams = new URLSearchParams(window.location.search);
            const action = urlParams.get('action');
            
            const titleElement = document.getElementById('successTitle');
            const messageElement = document.getElementById('successMessage');
            
            switch(action) {
                case 'signup':
                    titleElement.textContent = 'Welcome!';
                    messageElement.textContent = 'Your account has been created successfully. You can now start comparing AI responses.';
                    break;
                case 'update':
                    titleElement.textContent = 'Profile Updated!';
                    messageElement.textContent = 'Your profile information has been updated successfully.';
                    break;
                case 'password':
                    titleElement.textContent = 'Password Changed!';
                    messageElement.textContent = 'Your password has been updated successfully.';
                    break;
                default:
                    titleElement.textContent = 'Success!';
                    messageElement.textContent = 'Your operation completed successfully.';
            }
        });
    </script>
    
    <style>
        .success-content {
            text-align: center;
        }
        
        .success-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }
        
        .success-content h1 {
            font-family: var(--font-playfair);
            font-weight: bold;
            font-size: 2rem;
            color: var(--emerald);
            margin-bottom: 1rem;
        }
        
        .success-content p {
            color: var(--midnight);
            opacity: 0.8;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        
        .success-actions {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        
        .success-actions .auth-submit-btn {
            margin: 0;
        }
        
        @media (min-width: 480px) {
            .success-actions {
                flex-direction: row;
            }
        }
    </style>
</body>
</html>
